name: odab_ddokddok
description: Offline-first wrong-answer note coach app.
publish_to: none
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  go_router: ^16.0.0
  image_picker: ^1.1.2
  drift: ^2.29.0
  path: ^1.9.1
  path_provider: ^2.1.5
  sqlite3_flutter_libs: ^0.5.39
  firebase_core: ^3.15.2
  firebase_auth: ^5.6.3
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.10.0
  drift_dev: ^2.29.0

flutter:
  uses-material-design: true
